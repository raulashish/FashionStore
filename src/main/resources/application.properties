# Application Configuration
spring.application.name=fashion-store
server.port=2323

# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/fashion_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ashish$1992

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# HikariCP Connection Pool Configuration (Optional)
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

#Spring Security Admine USER and PASSWORD
spring.security.user.name=admin
spring.security.user.password=admin123
